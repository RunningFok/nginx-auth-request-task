http:
  routers:
    main:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      middlewares:
        - auth-forward
      service: auth-service

  services:
    auth-service:
      loadBalancer:
        servers:
          - url: http://auth:5000
